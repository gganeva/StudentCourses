@using StudentCourses.Web.Models.Home
@model IEnumerable<CourseViewModel>

<h3>Registered Courses</h3>
<table cellpadding="5">
	<thead>
		<tr>
			<td width="150">Course</td>
			<td width="200">Description</td>
			<td width="100"></td>
		</tr>
	</thead>
	<tbody>
		@{
			foreach (var course in Model)
			{
				<tr>
					<td>@course.Title</td>
					<td>@course.Description</td>
					<td>
						@Ajax.ActionLink("Unregister", "UnregisterFromCourse",
								 new { id = course.Id },
								 new AjaxOptions()
								 {
									 HttpMethod = "POST",
									 Confirm = "Are you sure you wish to unregister from course \"" + course.Title + "\"?",
									 InsertionMode = InsertionMode.Replace,
									 UpdateTargetId = "registeredCourses"
								 })
					</td>
				</tr>
			}
		}
	</tbody>
</table>